#take the time series and perform the optimal embedding procedure
#manually

# save the time series in the embedded array

#create intervall splitting with the moving window size
#cheapest way would be to put the dimensions through one after another with fixed size

function multi_dim_split(X,W)
    